---
title: "【2026年最新】Cursor × MCP導入で陥る「コンテキスト汚染」とセキュリティ地雷の回避策"
date: "2026-07-27"
tags: ["Cursor", "MCP", "DevOps", "Security"]
description: "AIエディタCursorとMCP（Model Context Protocol）の本格運用で発生するトークン高騰やセキュリティ事故を防ぐ実践的な設定ハックを解説します。"
---

AIエディタの標準となった **Cursor** と、外部データソースやツールを柔軟に接続する **MCP（Model Context Protocol）** の組み合わせは強力です。

しかし、チーム全体で本格運用を始めると、「トークン消費の急増」「プロンプト応答の低速化」「意図しない秘匿情報のLLM送信」といったトラブルが多発します。

本記事では、Cursor × MCP 運用において現場で発生するトラブルの背景と、それを防ぐための実践的な設定ハックを解説します。

---

## 現場で発生する3つの「MCP地雷」

MCPは強力ですが、標準設定のままチーム開発に投入すると以下の問題が発生します。

### 1. レスポンス肥大化による「コンテキスト汚染」とコスト暴走
MCPサーバー（例: DBクエリツールやログ検索ツール）が巨大なJSONやログを出力した場合、それがそのままCursorのコンテキストウィンドウに読み込まれます。結果としてコンテキストの上限を圧迫し、モデルの推論精度低下やAPIコストの急増を招きます。

### 2. `.cursorrules` とMCPの競合
プロジェクト固有の指示を書く `.cursorrules` と、MCP経由で取得した動的コンテキストが衝突し、AIが指示を無視する現象が発生します。

### 3. 環境変数・秘匿情報の漏洩リスク
MCPサーバーのローカル実行時、環境変数（`.env`）に含まれるAPIキーやDB接続文字列が、不適切にプロンプトへ組み込まれて外部LLMへ送信される危険性があります。

---

## 対策1: MCP出力のフィルタリングとサマリー化

MCPサーバー側で返すレスポンスの上限サイズを設定し、必要なフィールドのみに絞り込むプロキシ構成を挟むことが有効です。

```json
// mcp-config.json の設定例
{
  "mcpServers": {
    "db-query": {
      "command": "node",
      "args": ["./mcp-db-server.js"],
      "env": {
        "MAX_RESPONSE_BYTES": "4096",
        "EXCLUDE_FIELDS": "password,secret_key,access_token"
      }
    }
  }
}
